'use strict';
//const { saveTweet} = require('./model.js');


 function onSuma(data) {
    
 }

//con el on establecemos un listener
process.on('message', (x,y)=> {
    
   /* entiendo que aqui se va a esperar que lleguen los dos 
    valores a suma */
});